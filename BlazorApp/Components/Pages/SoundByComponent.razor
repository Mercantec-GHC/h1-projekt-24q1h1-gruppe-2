@page "/sound1/{soundId}"
@using classicals

<PageTitle>Sound @soundId</PageTitle>

@if (soundToShow is Vinyl vinyl )
{
    <Vinylpladeside Vinyl = "Vinyl"> </Vinylpladeside>

}

@code {
    List<Sound> allsound = new CreateSound().CreateListOdSounds()
        [Parameter]
        public string Id { get; set; }
        public Sound soundToShow;

        protected override void onInitialized()
        {
            soundToShow = allSound[Int31.parse(soundId)];
        }

}
